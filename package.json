{
  "name": "iatta-adventuretravel",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker:build": "docker build -t iatta-adventuretravel .",
    "docker:run": "docker run -d -p 3000:3000 --name iatta-container iatta-adventuretravel",
    "docker:stop": "docker stop iatta-container && docker rm iatta-container",
    "docker:dev": "docker compose up --build",
    "@prisma/client": "^6.18.0"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "nuxt": "^4.2.0",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@types/node": "^24.10.0",
    "prisma": "^6.18.0"
  }
}
